export const metadata = {
  title: 'Tüm Programlarımız',
  description: 'Fitness, yoga, pilates, kick boks, MMA ve personal training programları. Hedefinize en uygun dersi seçin.',
};

export default function ProgramlarLayout({ children }) {
  return children;
}
